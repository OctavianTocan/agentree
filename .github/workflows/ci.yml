name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v3

      - name: Install dependencies
        run: uv sync

      - name: Check formatting
        run: uv run ruff format --check pdfindex tests

      - name: Lint
        run: uv run ruff check pdfindex tests

      - name: Type check
        run: uv run ty check pdfindex tests

      - name: Test
        run: uv run pytest -q
